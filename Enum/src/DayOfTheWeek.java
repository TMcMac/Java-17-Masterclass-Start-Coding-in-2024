public enum DayOfTheWeek {
    // Enum is a great way to have a list of constants
    // You can also add functions as the instances of enum for each day are just like instances of a class
    SUNDAY, MONDAY, TUESDAY, WEDNESDAY, THURSDAY, FRIDAY, SATURDAY
}
